---
layout: post
title: "Rejstřík"
order: 1000
---

🚧 **Tato kapitola se teprve připravuje.** 

---

Rejstřík níže pokrývá kapitoly probrané k dnešnímu dni, ale s přibývajícími kapitolami se bude dál rozšiřovat a přeskupovat. Pokud v něm chybí pojem, který hledáte, zkuste postranní navigaci nebo vyhledávání.

---

Rejstřík je seřazený abecedně podle pojmu. U každého najdete stručné vysvětlení a kapitolu, kde se pojem poprvé zavádí nebo podrobně probírá — pokud se k němu kniha vrací i jinde, je uvedena jen ta nejdůležitější kapitola.

---

## A

| Pojem | Kde |
|---|---|
| `abstract` (třída, metoda) | **Abstraktní třídy a rozhraní** |
| `Action<>` | **Delegáti a vlastní události** |
| algoritmizace | **Algoritmizace** |
| algoritmus, vlastnosti algoritmu | **Co je algoritmus** |
| `and`, `or`, `not` (pattern matching) | **Pattern matching** |
| `Array` (třída, `Sort`/`Reverse`/`IndexOf`/`Copy`) | **Pole** |
| asymptotická notace (O-notace) | **Složitost algoritmů** |

## B

| Pojem | Kde |
|---|---|
| `base` | **Dědičnost** |
| BCL (Base Class Library) | **.NET C# a vývojové prostředí** |
| bitové operátory (`&`, `\|`, `^`, `~`, `<<`, `>>`) | **Operátory a výrazy** |
| `bool` | **Proměnné a datové typy** |
| boxing | **Generika** |
| `break` / `continue` | **Cykly** |
| `byte` | **Proměnné a datové typy** |

## C

| Pojem | Kde |
|---|---|
| camelCase | **Proměnné a datové typy** |
| `char` | **Proměnné a datové typy** |
| CIL / IL (mezikód) | **.NET C# a vývojové prostředí** |
| `class` | **Třída a objekt** |
| CLR (Common Language Runtime) | **.NET C# a vývojové prostředí** |
| Class Library (projekt) | **Sdílení kódu — knihovny a NuGet** |
| `Console` (`WriteLine`, `Write`, `ReadLine`, `Read`) | **Vstup a výstup** |
| `const` | **Proměnné a datové typy** |
| konstruktor, přetěžování konstruktorů, `: this(...)` | **Třída a objekt** |
| `Convert` (`ToInt32`, `ToDouble`...) | **Operátory a výrazy**, **Předdefinované metody** |

## D

| Pojem | Kde |
|---|---|
| datové struktury (přehled) | **Datové struktury a kolekce** |
| `DateTime` | **Předdefinované metody** |
| dekompozice problému | **Dekompozice problému** |
| dekompozice a návrh aplikace (WFA) | **Dekompozice a návrh aplikace** |
| delegát | **Delegáti a vlastní události** |
| determinovanost | **Co je algoritmus** |
| `Dictionary<TKey, TValue>` | **List a Dictionary** |
| `DivideByZeroException` | **Výjimky** |
| `do-while` | **Cykly** |
| dokumentační komentáře (`///`) | **XML dokumentační komentáře** |

## E

| Pojem | Kde |
|---|---|
| enkapsulace — viz zapouzdření | **Zapouzdření** |
| `enum` | **Struktura a enumerace** |
| `event` | **Události a event handlery**, **Delegáti a vlastní události** |
| `EventHandler<T>`, `EventArgs` | **Delegáti a vlastní události** |
| `Exception`, `try`/`catch`/`finally` | **Výjimky** |

## F

| Pojem | Kde |
|---|---|
| faktoriál (rekurzivní příklad) | **Rekurze** |
| `File` (`ReadAllText`, `WriteAllLines`...) | **Práce se soubory** |
| `FileInfo` / `DirectoryInfo` | **Cesty a adresáře** |
| `for` | **Cykly** |
| `foreach` | **Cykly** |
| `Func<>` | **Delegáti a vlastní události** |

## G

| Pojem | Kde |
|---|---|
| garbage collector (GC), generace GC | **Garbage collector** |
| generika, typový parametr `T` | **Generika** (úvod už v **List a Dictionary**) |
| Git, verzování | **Verzovací systémy a Git** |
| `GroupBy` | **Lambda funkce a LINQ** |

## H

| Pojem | Kde |
|---|---|
| halda (heap) | **Zásobník a halda** |
| `HashSet<T>` | **Datové struktury a kolekce** |
| hodnotové typy | **Hodnotové a referenční typy** |
| hromadnost (vlastnost algoritmu) | **Co je algoritmus** |

## I

| Pojem | Kde |
|---|---|
| `IComparable<T>` | **Abstraktní třídy a rozhraní** |
| `IDisposable`, `Dispose()` | **Garbage collector** |
| `IEnumerable<T>` | **Abstraktní třídy a rozhraní** |
| `if` / `else if` / `else` | **Podmínky** |
| implicitní a explicitní konverze | **Proměnné a datové typy** |
| `int` | **Proměnné a datové typy** |
| interface (rozhraní) | **Abstraktní třídy a rozhraní** |
| IntelliSense | **Vývojové prostředí** |
| `internal` | **Zapouzdření** |

## K

| Pojem | Kde |
|---|---|
| kódování (UTF-8, BOM) | **Práce se soubory** |
| kompozice vs. dědičnost | **Kompozice vs. dědičnost** |
| konečnost (vlastnost algoritmu) | **Co je algoritmus** |
| konzole — viz `Console` | **Vstup a výstup** |
| kritéria výběru datové struktury | **Datové struktury a kolekce** |

## L

| Pojem | Kde |
|---|---|
| lambda výraz | **Lambda funkce a LINQ** |
| LINQ (`Where`, `Select`, `OrderBy`, `GroupBy`, `Any`, `All`...) | **Lambda funkce a LINQ** |
| `List<T>` | **List a Dictionary** |
| líné vyhodnocování (`&&`, `\|\|`) | **Operátory a výrazy** |

## M

| Pojem | Kde |
|---|---|
| `Main()` | **Základy jazyka C#** |
| `Math` (`Sqrt`, `Round`, `Pow`...) | **Předdefinované metody** |
| metoda, deklarace a volání | **Metody** |
| metoda, návrh (jedna odpovědnost, signatura) | **Vlastní metody** |
| modifikátory přístupu (`public`/`private`/`protected`/`internal`) | **Metody**, **Zapouzdření** |
| MSTest, `[TestClass]`, `[TestMethod]`, `Assert` | **Testování** |

## N

| Pojem | Kde |
|---|---|
| `namespace` | **Základy jazyka C#** |
| nekonečná smyčka | **Cykly** |
| `new` (operátor) | **Datové struktury a kolekce**, **Třída a objekt** |
| nullable typy (`int?`, `??`, `?.`, `??=`) | **Nullable typy a operátory ?., ??, ??=** |
| NuGet | **Sdílení kódu — knihovny a NuGet** |

## O

| Pojem | Kde |
|---|---|
| objekt (instance třídy) | **Objektově orientované programování**, **Třída a objekt** |
| OOP, čtyři pilíře | **Objektově orientované programování** |
| `out` (parametr) | **Parametry a návratové hodnoty** |
| `override` | **Dědičnost**, **Polymorfismus** |

## P

| Pojem | Kde |
|---|---|
| paměťová složitost | **Složitost algoritmů** |
| pattern matching | **Pattern matching** |
| `Path` (třída) | **Cesty a adresáře** |
| `Pen`, `Brush`, `Graphics` | **Základy práce s grafikou** |
| pole (`array`) | **Pole** |
| polymorfismus | **Polymorfismus** |
| pozdní vazba (late binding) | **Polymorfismus** |
| property (vlastnost, `get`/`set`) | **Třída a objekt** |
| pseudokód | **Algoritmizace** |

## R

| Pojem | Kde |
|---|---|
| `Random` | **Předdefinované metody** |
| readonly pole | **Kompozice vs. dědičnost** |
| `ref` (parametr) | **Parametry a návratové hodnoty** |
| referenční typy | **Hodnotové a referenční typy** |
| rekurze, base case | **Rekurze** |
| rezultativnost (vlastnost algoritmu) | **Co je algoritmus** |
| řadící algoritmy (Bubble/Selection/Insertion Sort) | **Řadící algoritmy** |

## S

| Pojem | Kde |
|---|---|
| signatura metody | **Vlastní metody** |
| `SingleOrDefault` / `FirstOrDefault` | **Lambda funkce a LINQ** |
| skládání dotazů (LINQ chaining) | **Lambda funkce a LINQ** |
| smoke test (a proč nestačí) | **Testování** |
| správnost (vlastnost algoritmu) | **Co je algoritmus** |
| `StackOverflowException` | **Zásobník a halda**, **Rekurze** |
| `static` vs. instanční členy | **Třída a objekt** |
| stavební kameny GUI — viz WinForms | **Grafické aplikace — WinForms** |
| `string`, immutabilita | **Práce s řetězcem** |
| `StringBuilder` | **Práce s řetězcem** |
| `struct` | **Struktura a enumerace** |
| `StreamReader` / `StreamWriter` | **Vstupní a výstupní proudy** |
| `switch`, `switch` expression | **Podmínky** |

## T

| Pojem | Kde |
|---|---|
| technický dluh | **Životní cyklus vývoje programu** |
| ternární operátor (`?:`) | **Operátory a výrazy**, **Podmínky** |
| `this` | **Třída a objekt**, **Grafické aplikace — WinForms** |
| `Timer` | **Pokročilé komponenty**, **Animace a časovač** |
| typový parametr `T` | **List a Dictionary**, **Generika** |

## U

| Pojem | Kde |
|---|---|
| `using` (import jmenného prostoru) | **Základy jazyka C#** |
| `using` (blok / deklarace, automatický `Dispose`) | **Vstupní a výstupní proudy**, **Garbage collector** |

## V

| Pojem | Kde |
|---|---|
| validace vstupu, `TryParse` | **Vstup a výstup**, **Parametry a návratové hodnoty** |
| vícerozměrná pole (pravoúhlé, zubaté) | **Pole** |
| `virtual` | **Dědičnost**, **Polymorfismus** |
| vlastnosti algoritmu (hromadnost, determinovanost...) | **Co je algoritmus** |
| `void` | **Metody**, **Parametry a návratové hodnoty** |
| výjimka (exception) | **Výjimky** |
| vývojový diagram | **Vývojové diagramy** |
| vývojové prostředí (IDE), Visual Studio | **Vývojové prostředí** |

## W

| Pojem | Kde |
|---|---|
| `where T : ...` (omezení generik) | **Generika** |
| WinForms, komponenty | **Grafické aplikace — WinForms**, **Základní komponenty** |

## Z

| Pojem | Kde |
|---|---|
| zapouzdření | **Zapouzdření** |
| zásobník (stack, paměťová oblast) | **Zásobník a halda** |
| zásobník (`Stack<T>`, datová struktura) | **Datové struktury a kolekce** |
| životní cyklus vývoje programu | **Životní cyklus vývoje programu** |